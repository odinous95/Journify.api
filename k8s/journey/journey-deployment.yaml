apiVersion: apps/v1
kind: Deployment
metadata:
  name: journey-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: journey
  template:
    metadata:
      labels:
        app: journey
    spec:
      containers:
      - name: journey
        image: levantisk/journify-step:latest
        ports:
        - containerPort: 8080
        env:
        - name: PostgreSqlConnection
          valueFrom:
            secretKeyRef:
              name: user-db-secret
              key: PostgreSqlConnection

